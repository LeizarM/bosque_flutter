import 'package:flutter/material.dart';

class ControlCombustibleMaquinaMontacargaScreen extends StatelessWidget {
  const ControlCombustibleMaquinaMontacargaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 200,
      fallbackWidth: 200,
      color: Colors.blue,
      strokeWidth: 2,
      child: Center(
        child: Text(
          'Control Combustible Maquina Montacarga',
          style: TextStyle(fontSize: 16, color: Colors.white),
        ),
      ),
    );
  }
}